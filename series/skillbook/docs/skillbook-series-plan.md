# Verdent Skillbook — 短视频系列内容方案

投放平台：X / TikTok / IG
节奏：隔天一条，共 3 条
单条时长：24s
画幅：9:16，1080×1920
声音：默认静音自动播放，全片纯视觉/文字驱动，不依赖配音

系列叙事：`想清楚（Plan）→ 设计出来（Design）→ 变成代码（AI2UI）`
三条合起来讲一条完整链路：从模糊想法到可运行产品。

---

## 通用时间轴（三集共用）

```
0.0–3.2s    Scene 0  hook
3.2–6.8s    Scene 1  raw input
6.8–12.4s   Scene 2  处理过程（doc/生成 mockup）
12.4–17.0s  Scene 3  artifact 揭晓（全片最大重音）
17.0–24.0s  Scene 4  before/after/next + 收尾
```

## 通用视觉规则

- 色板顺序固定：sage → cream → canvas → plum → sage
- 每集只替换：eyebrow / headline / raw input 内容 / toolbar 状态文案 / chip 标签 / before-after 文案
- `next-tag` 串联三集：EP001 结尾指向 EP002，EP002 结尾指向 EP003，EP003 收尾不再预告
- 字段/chip 用统一的渐进弹入 + 短暂高光扫过效果，营造"刚被生成"的实时感
- 全片仅一次「揭晓闪光」，出现在 artifact 场景开场（12.4s 附近）

---

## EP001 — Plan Mode

**一句话转化**：模糊 brief → 可执行的内容计划

| 帧 | 背景色 | 屏上内容 |
|---|---|---|
| 0 hook | sage | `Vague brief.` / **`Content plan.`** / sub: Plan Mode turns noise into a plan you can run today. |
| 1 raw input | cream | quote: "Launch content. No angle. No first step." / tags: no angle / no sequence / no next step |
| 2 处理 | canvas | toolbar 状态：parsing brief → extracting goal → mapping angle → listing assets → sequencing checklist → plan ready；字段：Goal / Angle / Assets / Checklist / Next |
| 3 artifact | plum | `Content` **`Plan.`** / sub: Goal, angle, assets, first move. All in one place. / chips: Goal / Angles / Assets / Checklist |
| 4 收尾 | sage | Before: a blurry content ask / After: an executable content plan / Next: first asset queued / endline: One skill. One workflow. One artifact. / next-tag: NEXT: DESIGN |

发布文案方向：「你只说了一句『做点内容』，然后呢？」

---

## EP002 — Design

**一句话转化**：模糊的设计诉求 → 可执行的 UI 方案

| 帧 | 背景色 | 屏上内容 |
|---|---|---|
| 0 hook | sage | `Vague ask.` / **`A shippable design.`** / sub: Design Mode turns a request into a UI you can build. |
| 1 raw input | cream | quote: "Make it look better. No layout. No system." / tags: no hierarchy / no tokens / no states |
| 2 处理 | canvas | toolbar 状态：parsing brief → mapping layout → defining tokens → building components → covering states → design ready；字段：Layout / Palette·Type / Components / States |
| 3 artifact | plum | `Design` **`System.`** / sub: Layout, tokens, components, states. All in one place. / chips: Layout / Tokens / Components / States |
| 4 收尾 | sage | Before: a vague design ask / After: a buildable UI system / Next: first component queued / endline: One skill. One workflow. One artifact. / next-tag: NEXT: AI2UI |

发布文案方向：「『好看点』不是设计需求，这才是。」

---

## EP003 — AI2UI

**一句话转化**：一张图 → 能跑的界面代码

| 帧 | 背景色 | 屏上内容 |
|---|---|---|
| 0 hook | sage | `One image.` / **`Working UI.`** / sub: AI2UI turns a picture into code that runs. |
| 1 raw input | cream | 用简化线框图代替真实截图（规避版权）/ quote: "Just this screenshot. No spec. No code." / tags: no markup / no styles / no logic |
| 2 处理 | canvas | toolbar 状态：reading image → mapping layout → generating components → writing styles → wiring interactions → build ready；字段：Layout / Components / Styles / Interactions |
| 3 artifact | plum | `Working` **`UI.`** / sub: Structure, style, interaction. Ready to run. / chips: Layout / Components / Styles / Interactions |
| 4 收尾 | sage | Before: a static image / After: a running component / Next: data wiring queued / endline: One skill. One workflow. One artifact. / next-tag: （不预告，系列收尾） |

发布文案方向：「一张图，怎么变成能点的界面？」

---

## 发布排期

| 日期 | 内容 |
|---|---|
| Day 1 | EP001 Plan Mode |
| Day 3 | EP002 Design |
| Day 5 | EP003 AI2UI |

三条发完后可剪成一条合集长视频二次分发（Plan → Design → AI2UI 完整链路）。
